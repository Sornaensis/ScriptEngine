#ifndef __SCRIPT_TOKENIZER_INCLUDED__
#define __SCRIPT_TOKENIZER_INCLUDED__

#include <string>
#include <vector>

using namespace std;

class ScriptTokenizer {
public:
    vector<string> tokenizeText(string text);

};

#endif

